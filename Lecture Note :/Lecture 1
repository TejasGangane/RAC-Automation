#Python Program to Find the maximum number in an array.

def max(arr,n):
    max=arr[0]
    for i in range(1,n):
        if arr[i]>max:
            max=arr[i]
    return max

arr=[1,2,7,3,8]
n=len(arr)
ans=max(arr,n)
print("The maximum number is ",ans)